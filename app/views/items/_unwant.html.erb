<%= link_to "" , 
    ownership_path(current_user.wants.find_by(:item_id => item.id) , 
      item_id: item.id , type: "Want") ,
    :class => "want-#{item.id} btn-want btn btn-danger btn-sm want_it",
    :remote => true,
    :method => :delete %>
